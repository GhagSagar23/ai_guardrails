name: ai_guardrails_server
description: >-
  Shelf middleware and standalone HTTP server wrapping AiGuard — scan
  endpoints, OpenAI-compatible proxy, JSON policy config, Docker-ready.
version: 0.1.0
repository: https://github.com/GhagSagar23/ai_guardrails/tree/master/packages/ai_guardrails_server
issue_tracker: https://github.com/GhagSagar23/ai_guardrails/issues
topics:
  - ai
  - security
  - guardrails
  - server
  - shelf

environment:
  sdk: ^3.5.0

resolution: workspace

dependencies:
  ai_guardrails: ">=1.0.0 <2.0.0"
  http: ^1.2.0
  shelf: ^1.4.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
