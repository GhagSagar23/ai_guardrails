# ai_guardrails icon

A shield (safety) carrying a small neural/circuit node cluster (AI) and a check
mark (validated / safe) — the package's job: guarding AI inputs and outputs.

## Palette

| Role        | Hex       |
|-------------|-----------|
| Indigo (top-left)  | `#4338CA` |
| Teal (bottom-right) | `#0D9488` |
| Nodes + check      | `#FFFFFF` |

Self-contained SVG: inline gradient/paths only, no external refs, fonts, or
`<image>`. Legible down to 64px.

The main README references it via the raw master URL:
`https://raw.githubusercontent.com/GhagSagar23/ai_guardrails/master/icon/ai_guardrails.svg`
